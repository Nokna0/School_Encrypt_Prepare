import time
import json
import hashlib

class Block:
  pass # 블록이라는 클래스를 만들고 그걸 프리셋으로 정한다

class Blockchain:
  pass # 블록체인이라는 클래스를 만들고 그걸 프리셋으로? 만든다

blockchain = Blockchain()
block = Block()

# 여기에 코드를 작성한다
# 테스트하는 과정을 여기에 코드로 구현하고 위에 클래스에 그 과정을 전부 만든다

